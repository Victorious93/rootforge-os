"""RootForge OS Python package root.

Subpackages land here as behavior migrates out of the standalone
usr/local/bin/*.sh scripts (see docs/IMPLEMENTATION_PLAN.md). Existing
scripts keep working unmodified until each is individually wrapped —
nothing under usr/local/bin/ imports from here yet.
"""
