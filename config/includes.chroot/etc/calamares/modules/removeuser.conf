# RootForge OS — Calamares removeuser module
# Victorious Framework | Origin Source Labs
#
# Removes the ephemeral live user account after the real user is created
# during disk install. live-build creates a 'user' account for the live session.

---
username: user
